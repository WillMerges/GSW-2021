wget https://dl.influxdata.com/influxdb/releases/influxdb_1.1.1_amd64.deb
sudo dpkg -i influxdb_1.1.1_amd64.deb

sudo service influxdb start/stop

sudo influxd config

sudo influxd -config path/to/.conf


> influx
# starts influx client

create database [whatever]
use database [name]

use udp line protocol to send data
