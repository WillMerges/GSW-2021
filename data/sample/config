# SAMPLE CONFIGURATION
# assumes in order
# this is what packet's coming from the receiver should look like
# if measurements have the same name the latest one's size is used (furthest down in the file)!

# these need to be set at some point!
protocol = udp
addr = 127.0.0.1
port = 8080

# device name
name = sample_device

# [measurement name] [total measurement size in bytes (including padding)] [most sig padding (bits)] [least sig (bits)] [optional type of int or float]
TEST 4 0 0 int
